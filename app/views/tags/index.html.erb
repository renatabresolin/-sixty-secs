<div class="banner" style="background-image: url(<%= image_path("backg-home.png") %>);">

  <div class="home-cards">
    <h3 class="text-md-left mt-5 ml-5">
      <span class="letters">Qual a ocasião?</span>
    </h3>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/animejs/2.0.2/anime.min.js"></script>
    
    <div class="tag-container">
      <% @tags.each do |tag| %>
          <div class="occasions">
            <p> <%= link_to tag.content, apparels_path(tag_id: tag.id)%></p>
          </div>
      <% end %>
    </div>
  </div>

</div>



